// Strings françaises — locale par défaut MVP.
// L'anglais sera ajouté Sprint 2 (i18next + détection locale device).

export const fr = {
  splash: {
    tagline: 'DOUMASSI',
  },
} as const;

export type Translations = typeof fr;
