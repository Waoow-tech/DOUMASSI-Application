// Tab Home — URL : /feed (le groupe (tabs) est strippé de l'URL).
// Tickets E2-03 (Sprint 1) + E8-04 (refacto TabBar Sprint 3).

import { FeedScreen } from '@/features/feed/screens/FeedScreen';

export default function FeedRoute() {
  return <FeedScreen />;
}
